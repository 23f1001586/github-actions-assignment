name: Run Assignment Action

on:
  workflow_dispatch: # This adds a manual button to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1001586@ds.study.iitm.ac.in
        run: echo "Hello, world!"
